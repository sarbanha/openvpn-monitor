# OpenVPN Monitor Configuration
# Copy this file to .env and configure for your environment

# ============================================================================
# OpenVPN Connection Settings
# ============================================================================

# OpenVPN management interface host
OPENVPN_NC_HOST=127.0.0.1

# OpenVPN management interface port
OPENVPN_NC_PORT=7505

# OpenVPN service name (systemd)
OPENVPN_SERVICE=openvpn-server@hd

# ============================================================================
# Email Configuration
# ============================================================================

# Enable/disable email notifications
OPENVPN_EMAIL_ENABLED=false

# SMTP Server Configuration
OPENVPN_SMTP_HOST=localhost
OPENVPN_SMTP_PORT=25

# Connection Security (choose one):
# - none: Plain connection, no encryption (port 25, local servers)
# - starttls: Upgrade connection with STARTTLS (port 587 or 25)
# - tls: Direct SSL/TLS connection (port 465)
OPENVPN_SMTP_SECURITY=none

# SMTP Authentication (optional - leave empty if not required)
OPENVPN_SMTP_USERNAME=
OPENVPN_SMTP_PASSWORD=

# Email Addresses
OPENVPN_EMAIL_FROM=openvpn-monitor@localhost
# Comma-separated list of recipient emails
OPENVPN_EMAIL_TO=

# ===================================
# Example Configurations
# ===================================

# Example 1: Gmail (uses STARTTLS on port 587)
# OPENVPN_EMAIL_ENABLED=true
# OPENVPN_SMTP_HOST=smtp.gmail.com
# OPENVPN_SMTP_PORT=587
# OPENVPN_SMTP_SECURITY=starttls
# OPENVPN_SMTP_USERNAME=your-email@gmail.com
# OPENVPN_SMTP_PASSWORD=your-app-password
# OPENVPN_EMAIL_FROM=openvpn-monitor@yourdomain.com
# OPENVPN_EMAIL_TO=admin1@example.com,admin2@example.com,admin3@example.com

# Example 2: Office 365 (uses STARTTLS on port 587)
# OPENVPN_EMAIL_ENABLED=true
# OPENVPN_SMTP_HOST=smtp.office365.com
# OPENVPN_SMTP_PORT=587
# OPENVPN_SMTP_SECURITY=starttls
# OPENVPN_SMTP_USERNAME=your-email@yourcompany.com
# OPENVPN_SMTP_PASSWORD=your-password
# OPENVPN_EMAIL_FROM=openvpn-monitor@yourcompany.com
# OPENVPN_EMAIL_TO=it-team@yourcompany.com,sysadmin@yourcompany.com

# Example 3: Local Mail Server (no encryption)
# OPENVPN_EMAIL_ENABLED=true
# OPENVPN_SMTP_HOST=localhost
# OPENVPN_SMTP_PORT=25
# OPENVPN_SMTP_SECURITY=none
# OPENVPN_EMAIL_FROM=openvpn-monitor@localhost
# OPENVPN_EMAIL_TO=root@localhost,admin@localhost

# Example 4: Direct TLS (port 465)
# OPENVPN_EMAIL_ENABLED=true
# OPENVPN_SMTP_HOST=smtp.example.com
# OPENVPN_SMTP_PORT=465
# OPENVPN_SMTP_SECURITY=tls
# OPENVPN_SMTP_USERNAME=user@example.com
# OPENVPN_SMTP_PASSWORD=password
# OPENVPN_EMAIL_FROM=openvpn-monitor@example.com
# OPENVPN_EMAIL_TO=admin@example.com
# OPENVPN_SMTP_USE_TLS=false
# OPENVPN_EMAIL_FROM=openvpn-monitor@localhost
# OPENVPN_EMAIL_TO=root@localhost,admin@localhost

# Example 4: SendGrid
# OPENVPN_EMAIL_ENABLED=true
# OPENVPN_SMTP_HOST=smtp.sendgrid.net
# OPENVPN_SMTP_PORT=587
# OPENVPN_SMTP_USE_TLS=true
# OPENVPN_SMTP_USERNAME=apikey
# OPENVPN_SMTP_PASSWORD=your-sendgrid-api-key
# OPENVPN_EMAIL_FROM=openvpn-monitor@yourdomain.com
# OPENVPN_EMAIL_TO=alerts@yourdomain.com,oncall@yourdomain.com

